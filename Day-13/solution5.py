word_per_page = 0
pages = int(input("Number of pages: "))
word_per_page = int(input("Number of words per page: "))
total_words = pages * word_per_page
print(total_words)

print(f"pages: {pages}")
print(f"word_per_page: {pages}")
# if we print the every var,we can find the vaue of each var
# there is double equal